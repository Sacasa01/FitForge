meta {
  name: Recommend Unauthorized
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/exercises/recommend?muscleGroup=chest
  body: none
  auth: none
}

params:query {
  muscleGroup: chest
}

tests {
  test("rejects unauthenticated requests", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
