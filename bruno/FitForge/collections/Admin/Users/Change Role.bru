meta {
  name: Change Role
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/admin/users/1/role
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "role": "admin"
  }
}
